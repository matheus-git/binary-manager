pub struct EEhsize {
    pub raw: [u8; 2],
    pub describe: String
}

impl EEhsize {
    pub fn new(raw: [u8; 2]) -> Self {
        Self { 
            raw, 
            describe: "".to_string() 
        }
    }
}
